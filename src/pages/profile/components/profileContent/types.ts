import ProfileHeader from '../profileHeader/profileHeader'
import ProfileMain from '../profileMain/profileMain'
import PofileFooter from '../profileFooter/profileFooter'

export type Props = {
  header: ProfileHeader,
  main: ProfileMain,
  footer: PofileFooter
}
